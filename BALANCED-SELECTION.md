# Version 2 change

This package includes balanced character selection.

For each selected character set:

1. All selected characters are shuffled.
2. Each character is removed from the shuffled bag as it is used.
3. The bag is refilled and reshuffled only after every selected character has appeared.

The HTML and CSS are unchanged from the previous package.
